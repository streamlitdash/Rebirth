# Disabled recovered core provenance
#
# s02_pipeline_part_1.py.disabled is the exact available recovery source from
# s02_pipeline_part_1.py, preserved after LF normalization. Remove the leading
# "# " from nonblank lines (and "#" from blank lines) to reconstruct it.
# Normalized-LF SHA-256:
# 0aeb6be35c87496b3c7e76b943efca6f980cafe2a3b552cee4a5f6959972124a
#
# The fragment records the original ProductSpec P&L-formula metadata, the
# RiskChecker Age business-day arithmetic in risk_date_for(), and the original
# MRX File naming contract. It is provenance only: it is not active, imported,
# or allowed to change the live financial logic. The .py.disabled suffix makes
# it non-importable, and the generic _disabled publisher rule excludes it from
# Plotly deployment bundles.
